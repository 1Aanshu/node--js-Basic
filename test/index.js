// Create a library which finds the maximum and minimum value of an array
// Dont forget to pass the value as arguments(parameters)
// Use destructure concept in index.js

const { minMax } = require("./library.js");

const result1 = minMaxNumber.minMax();
console.log({ result1 });
